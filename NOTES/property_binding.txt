
Property binding :: 

    Bind a Property with html element.


    lets take 2 text boxes.

    try to enable.

    find difference between Property binding and interpolation

    interpolation :: When you need to concatenate strings, you must use 
    interpolation instead of property binding.

    Property Binding :: Property Binding is used when you have to set an 
    element property to a non-string data value
