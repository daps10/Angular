Style Binding in Angular.

- Difference between normal style and style binding.
    normal style means simple which we are using and style binding is another way we can define.
    

- Add dynamic style.

    dynamic style can get from the component,ts which is the dynamic 
    value.

- Update style with button click 

    For update you can use the function which will update the style and 
    other property.

- Add conditional style.

    conditional style like the we can gives the conditional approach from
    the component.ts file.